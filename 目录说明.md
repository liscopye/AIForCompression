# AIForCompression 目录说明

本文说明仓库根目录下每个一级文件夹的用途。第一次接触项目时先看根目录 `README.md`；准备运行正式评测时看 `docs/benchmark_reproduction_manifest.md`；编码 agent 还应先读 `AGENTS.md`。

## 目录总览

| 目录 | 作用 | 迁移建议 |
|---|---|---|
| `benchmark_protocols/` | 保存机器可读的正式评测协议。目前核心文件是 `objective_v1.json`，定义数据集、样本、指标和控制点要求。 | 必须保留。 |
| `checkpoints/` | 模型权重。包括 DCAE、LIC-HPCM、DCVC-RT、DCMVC、CAESAR 原始权重、ERA5 最佳微调权重和 Turb_Rot tuned 权重。代码通常只负责发现和加载，不会自动重建这些文件。 | 运行对应模型时必须复制；当前约 13 GB。 |
| `compression_pipeline/` | 项目核心 Python 框架：dataset adapter、canonical 数据结构、模型 view、codec wrapper、checkpoint 注册、指标、CAESAR/CRA5 runner 及 objective 数据处理。 | 必须保留；新增数据集或模型优先修改这里。 |
| `docs/` | 协议、复现、数据处理和环境启动文档。`docs/README.md` 是文档索引。 | 必须保留。 |
| `logs/` | 训练、下载和历史评测日志，不参与代码导入。部分日志用于追溯失败实验，但不会影响已有模型推理。 | 按需迁移；缺失不影响使用最终权重。 |
| `models/` | 上游/vendor 模型源码和 Git submodule，例如 CAESAR、DCAE、LIC-HPCM、DCVC、DCMVC、cuSZ-Hi 等。共享 pipeline 通过 wrapper 调用它们。 | 运行模型时必须保留并初始化所需 submodule；除修复模型兼容性外不要随意修改。 |
| `normalization/` | ERA5 adapter 使用的逐日 pressure/single-level mean/std JSON。它与通用 objective-v1 的 dataset-level normalization 不是同一个概念。 | 运行依赖该 adapter 的 ERA5 路径时保留。 |
| `scripts/` | 当前可执行入口：通用 pipeline、objective-v1、最佳 ERA5 CAESAR 微调链、外部 codec、数据准备和分析。每个文件的用途见 `scripts/README.md`。 | 必须保留。不要再加入一次性实验脚本。 |
| `tests/` | 当前框架和协议的回归测试。覆盖 pipeline、objective、CAESAR、ERA5、Turb_Rot、绘图和 Slurm 行为；索引见 `tests/README.md`。 | 开发和迁移验证时必须保留。 |
| `tools/` | 需要单独编译的底层工具。目前保存 nvJPEG/nvJPEG2000 C++ 源码及本机二进制。二进制可能不能跨机器直接使用。 | 源码保留；换机器后重新编译二进制。 |
| `unified_results/` | 正式主结果、CAESAR 微调专项、JSON summary、图表和 HTML；详见目录内 `README.md`。 | 查看或复核已有结果时保留。 |
| `utils/` | 被脚本和测试复用的数据下载、ERA5 shard、统计和绘图工具。与 `scripts/` 的区别是：这里主要提供辅助实现，而不是正式评测总入口。 | 必须保留；部分旧绘图工具可在后续单独审计。 |
| `wandb/` | Weights & Biases 本地运行缓存、离线记录和 debug 日志。最终 checkpoint 不依赖该目录。 | 通常不迁移；确认线上已同步或不需要恢复离线 run 后可删除。 |

## 隐藏目录

| 目录 | 作用 |
|---|---|
| `.git/` | Git 历史、分支和远端信息。需要继续提交和推送时保留。 |
| `.pytest_cache/` | pytest 自动生成的缓存，不属于项目数据，可以随时删除。 |

Python 运行还会在各源码目录生成 `__pycache__/`，它们同样可以随时删除，不应迁移或提交。

## 根目录文件

| 文件 | 作用 |
|---|---|
| `README.md` | 面向使用者的项目入口和快速开始。 |
| `AGENTS.md` | 面向编码 agent 的仓库规则、架构和关键路径。 |
| `requirements.txt` | Python 依赖列表。CUDA/C++ 扩展需按各模型与工具的说明单独构建。 |
| `pytest.ini` | pytest 配置。 |
| `.gitmodules` | vendor/submodule 地址。 |
| `.gitignore` | Git 忽略规则。 |

## 数据在哪里

大型原始数据不放在仓库内，统一位于 `/workspace/Data/`。例如：

```text
/workspace/Data/Turb_Rot_testset.npz
/workspace/Data/ERA5/
```

各数据集的数据来源和本地内容见 `docs/Data数据集.md`；正式评测的精确路径、shape、crop、通道组织和归一化规则见 `docs/benchmark_reproduction_manifest.md`；环境激活和测试命令见 `docs/环境激活与测试启动.md`。

## 最小迁移集合

若要在另一台机器继续开发和复现实验，至少需要：

```text
benchmark_protocols/
checkpoints/                 # 可以只复制准备运行的模型权重
compression_pipeline/
docs/
models/                      # 初始化所需 submodule
normalization/
scripts/
tests/
tools/                       # 换机器重新编译二进制
utils/
AGENTS.md
README.md
requirements.txt
pytest.ini
```

另外单独复制 `/workspace/Data/` 下需要的数据。`logs/` 和 `wandb/` 不是运行最终模型的必要条件。
